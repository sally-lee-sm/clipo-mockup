# 시작하기 — Mockup Starter Kit 사용법

올립이 CLIPO·HIAI 목업을 만들며 쌓아온 **원칙 + 스킬 + 확정 화면 + 제품 맥락**을 한 폴더에 모은 거예요.
이걸 가지고 Claude로 작업하면, 올립이 만든 것과 같은 결을 이어서 만들 수 있어요.

---

## 1. 이 폴더 받기 (clone 권장)

이 스타터킷은 **git 리포**예요. 두 방법 중 하나:

- **(권장) clone** — 사내 Gitea에서 clone 받으면 충돌 사본 없이 버전관리까지 돼요.
  ```
  git clone http://<본인아이디>@192.168.50.201:3000/Internal-Tool/mockup-starter-kit.git "내 목업"
  ```
- **(대안) 폴더 복사** — git이 낯설면 이 폴더를 내 드라이브에 **통째로 복사**(예: `샐리 목업/`)해서 그 안에서 작업해도 돼요. 단, **원본은 읽기전용 교본** — 여기서 바로 작업하지 마세요(여러 명이 같은 폴더를 건드리면 구글드라이브가 충돌 사본을 만들어요).

## 2. Claude를 내 폴더에서 열기

내 사본/클론 폴더를 작업 폴더로 열면 **`CLAUDE.md`(작업 원칙)와 `.claude/skills/`(목업 스킬)가 자동 로드**돼요. 따로 할 거 없어요.
- "CLIPO 〇〇 화면 만들어줘" 하면 Claude가 `clipo/레퍼런스화면/`의 확정 화면을 복제해서 시작해요.
- 미리보기는 채팅 오른쪽 패널에서 확인해요.

### (권장) Trellis 설치 — 원칙 자동 주입 + 작업 일지
설치하면 세션이 바뀌어도 원칙이 자동 주입되고, 작업 일지가 자동으로 쌓여요. **비개발자면 건너뛰어도 기본 작업은 됩니다** (CLAUDE.md가 원칙 문서를 가리켜 Claude가 읽어요). 여유될 때 한 번만:
```
uv tool install "git+http://<본인아이디>@192.168.50.201:3000/AI-Tools/trellis.git"
trellis connect          # 이 컴퓨터에서 1회
trellis attach           # 내 폴더에서 1회
```
자세한 건 팀에 공유된 Trellis 안내 참고.

## 3. 배포는 각자 개인 GitHub로
만든 목업을 웹에 올리고 싶으면 **본인 개인 GitHub**에 올리세요(올립을 거치지 않아요). 처음 1회만 Claude에게 "내 깃허브로 배포 세팅해줘" 하면 안내해줘요.

---

## 폴더 구조
```
mockup_starter_kit/
├── CLAUDE.md              ← 작업 원칙 요약 (자동 로드) + 상황별 문서 안내
├── 00_시작하기.md          ← 이 문서
├── 이어받기-템플릿.md       ← 진행 중 화면을 남에게 넘길 때 쓰는 양식
├── docs/                  ← Trellis 하네스
│   ├── principles.md      ← 원칙 (Trellis 설치 시 자동 주입 / 미설치면 CLAUDE.md가 가리킴)
│   ├── workflow.md        ← 일관성·변경 영향 체크·마무리 점검·이어받기
│   ├── checklist.md       ← 화면 출고 전 자가 점검표
│   ├── index.md           ← 상황별 "이 문서 보라" 길잡이
│   └── sessions/          ← 작업 일지 (자동 축적)
├── .claude/skills/        ← clipo-mockup / hiai-mockup 스킬 (자동 인식)
├── clipo/                 ← CLIPO 규칙·개발화면 기준·레퍼런스화면·이어받기·context
└── hiai/                  ← HIAI 동일 구조
```

## 클로드에 이렇게 입력하세요 (그대로 복붙 가능)

**① 맨 처음 한 번 (준비 확인)**
```
이 폴더는 목업 스타터킷이야. 00_시작하기.md랑 CLAUDE.md 먼저 읽고,
앞으로 이 지침대로 목업 작업할 거야. 준비되면 뭘 만들 수 있는지 알려줘.
```

**② 새 화면 만들기**
```
CLIPO 채점 화면을 만들고 싶어. 레퍼런스화면에서 비슷한 거 복제해서 시작해줘.
```
- 캡처나 기획 메모가 있으면 같이 붙여주면 훨씬 정확해요.

**③ 기존 화면 개선하기**
```
clipo/레퍼런스화면/creative_activity_list_v6_260507.html 이 화면에서
상단 안내 문구를 더 친절하게 바꾸고 싶어. 어떻게 하면 좋을지 같이 보자.
```
→ 라벨·문구를 여러 곳 바꿔야 하면 Claude가 "여기 6군데 있는데 다 바꿀까요?" 하고 먼저 물어봐요.

**④ 미리보기로 확인**
```
미리보기 띄워줘.
```

**⑤ 마무리 점검**
```
다 된 것 같아. 체크리스트로 점검해서 빠진 거 있는지 알려줘.
```
→ `docs/checklist.md`를 훑어서 "OK / 이건 고쳤어요"로 보고해줘요.

### 흐름 요약
받기(clone/복사) → 폴더 열기 → **①로 준비 확인** → ②/③로 만들기·고치기 → ④ 미리보기 반복 → ⑤ 점검 → (원하면) 개인 깃허브 배포

## 참고
- `.claude/skills/` 스킬 문서에 올립 PC 경로(`G:\…\clipo_mockup\`)가 적혀 있을 수 있어요. 내 사본엔 그 경로가 없지만, **항상 로드되는 `CLAUDE.md`가 원칙의 기준**이라 작업엔 지장 없어요. (헷갈리면 "이 폴더 기준으로 작업해줘" 한마디.)
- 막히면 Claude에게 "스타터킷 구조 설명해줘" 하면 이 문서를 읽고 안내해줘요.
